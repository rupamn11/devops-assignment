apiVersion: v1
kind: Service
metadata:
  name: todo-app-backend-service
  namespace: todo-app
spec:
  selector:
    app: todo-app
    component: backend
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP
